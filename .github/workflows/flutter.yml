name: Build & Release APK

on:
  workflow_dispatch:

jobs:
  build-apk:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4

      - name: Install Deps
        run: sudo apt-get update && sudo apt-get install -y ninja-build unzip

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.19.6'
          channel: 'stable'

      - name: Backup custom sources
        run: |
          cp -r lib/ /tmp/lib_backup/
          cp pubspec.yaml /tmp/pubspec_backup.yaml

      - name: Generate Android skeleton
        run: |
          # Create fresh Flutter project in temp
          flutter create --org com.smkiuh /tmp/flutter_proj
          # Copy Android + necessary files
          cp -r /tmp/flutter_proj/android ./android
          cp /tmp/flutter_proj/.gitignore .
          cp /tmp/flutter_proj/.metadata .
          cp /tmp/flutter_proj/.flutter-plugins-dependencies . 2>/dev/null || true
          # Restore our custom files
          rm -rf lib/
          cp -r /tmp/lib_backup/ lib/
          cp /tmp/pubspec_backup.yaml pubspec.yaml

      - name: Resolve Dependencies
        run: flutter pub get || echo "deps ok"

      - name: Build APK (Release)
        run: flutter build apk --release --build-number=${{ github.run_number }}

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-release.apk
          path: build/app/outputs/flutter-apk/app-release.apk

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: buku-kenangan-v${{ github.run_number }}
          name: Buku Kenangan SMK IHYAUL ULUM
          files: build/app/outputs/flutter-apk/app-release.apk
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        continue-on-error: true